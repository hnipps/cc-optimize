from __future__ import annotations


def score_with_llm_judge(task_id: str, session_trace: str) -> dict[str, float]:
    """Placeholder for optional LLM-as-judge scoring. Returns empty dict."""
    return {}
